# openresty-gateway

Release 0.1 data plane — OpenResty/Nginx reverse proxy with config generation and atomic reload.

**Sprint target:** Sprint 3 (virtual server, TLS, backend pool, health check, config generator).