# event-schema

JSON Schema source of truth for structured gateway events (access, security, health, audit).
SDK types are generated from this file — never hand-edited.

**Sprint target:** Sprint 2 (v0 schema), Sprint 5 (security event extension).

## Files

- `schema/event-v0.json` — event schema base