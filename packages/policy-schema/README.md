# policy-schema

JSON Schema source of truth for the declarative policy/config document (ADR-002).
SDK types are generated from this file — never hand-edited.

**Sprint target:** Sprint 2 (v0 schema), Sprint 5+ (WAF extension).

## Files

- `schema/policy-v0.json` — policy document schema (config bundle)