# sdk-typescript

Shared TypeScript types generated from `packages/policy-schema` and `packages/event-schema`.
Never hand-edited — regenerate via `make gen`.

**Sprint target:** Sprint 2 (initial generation).