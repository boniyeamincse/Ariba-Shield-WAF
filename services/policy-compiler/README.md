# policy-compiler

Go service that renders declarative policy documents (ADR-002) into gateway-native configuration.
Validates, signs, and produces immutable bundle hashes.

**Sprint target:** Sprint 3 (initial compiler), Sprint 5–6 (Phase 2 WAF extension).